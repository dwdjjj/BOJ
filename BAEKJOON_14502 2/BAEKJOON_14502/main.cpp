#include <queue>
#include <vector>
#include <iostream>
using namespace std;
int n, m;
vector<vector<int>> adj;

void bfs(){
    
}
int main(int argc, const char * argv[]) {
    cin >> n >> m;
    adj.resize(n, vector<int>(m, 0));
    
    for (int i=0; i<n; i++) {
        for (int j=0; j<n; j++) {
            <#statements#>
        }
    }
    return 0;
}
