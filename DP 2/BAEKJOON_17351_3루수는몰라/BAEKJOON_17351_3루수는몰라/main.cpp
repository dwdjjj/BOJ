#include <vector>
#include <algorithm>
#include <cstring>
#include <iostream>
using namespace std;
const int MAX_N = 500 + 3;
char arr[MAX_N][MAX_N];
int n;
int main(int argc, const char * argv[]) {
    ios::sync_with_stdio(0);
    cin.tie(0);
    for (int i=0; i<n; i++) {
        cin >> arr[i];
    }
    cin >> n;
    return 0;
}
